#include <iostream>
using namespace std;
int a[100];

int main(){
    int n,i,max,min;
    cout<<"Enter the number of elements";
    cin>>n;
    cout<<"Enter the elements";
    for(i=0;i<n;i++)
    cin>>a[i];
     if
     (a[0] < a[i])
      a[0] = a[i];
    cout<<"Maximum element is"<<a[0];
  
  
}